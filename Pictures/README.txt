Directory contains all image files used by the system.  We include the finished .jpg's and .png's along with any needed source files, such as any Photoshop .psd files.

Due to the number of images, we have 2 subdirectories.  Each directory contains the image files plus a pictorial directory of the image files.
